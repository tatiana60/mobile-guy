{% extends "base.html.twig" %}

{% block title %}Accueil{% endblock %}

{% block body %}
    <div id="contenu-accueil">
        <a id="background-slider" href="{{ path('product', {'slug':'coque-antichoc-pour-iphone-xr'}) }}">
            <img src="/img/Slider-image.png">
        </a>

        <div>
            <h1>Nouveautés</h1>
            <div id="cards-products">
                {% include 'list_products.html.twig' with {'product': 'i'} %}
            </div>
        </div>
        
    </div>
{% endblock %}
